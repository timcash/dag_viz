export class HUD {
    constructor() {
        // Simple UI for curviness presets if needed
    }

    getCurviness(): number {
        // For now, return a default or get from DOM
        return 10;
    }
}
